# Общие SSL-настройки (без certbot options-ssl-nginx.conf — его часто нет в Docker).
ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 1d;
